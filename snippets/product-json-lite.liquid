{% comment %}
  A lighter version of 'product-json.liquid' that only includes the fields required by productDetailForm.js

  Types are defined in @/types/shopify.ts with the 'Lite' prefix
{% endcomment %}

{% if product %}
  {% capture product_json %}
    {
      "id": {{ product.id }},
      "title": {{ product.title | json }},
      "handle": {{ product.handle | json }},
      "available": {{ product.available }},
      "variants": [
        {% for variant in product.variants %}
          {%- liquid
            capture compare_at_price_formatted
              render 'price-formatted', price: variant.compare_at_price
            endcapture

            capture price_formatted
              render 'price-formatted', price: variant.price
            endcapture
          -%}

          {
            "available": {{ variant.available }},
            "compare_at_price": {{ variant.compare_at_price | json }},
            "compare_at_price_formatted": {{ compare_at_price_formatted | json }},
            "id": {{ variant.id }},
            "options": [
              {% for option in variant.options %}
                {{ option | json }}
                {% unless forloop.last %},{% endunless %}
              {% endfor %}
            ],            
            "price": {{ variant.price | json }},
            "price_formatted": {{ price_formatted | json }},
            "title": {{ variant.title | json }}   
          }      
          {% unless forloop.last %},{% endunless %}
        {% endfor %}
      ],
      "options": [
        {% for option in product.options_with_values %}
          {{ option | json }}{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ],
      "options_with_values": [
        {% for option in product.options_with_values %}
          {
            "name": {{ option.name | json }},
            "position": {{ forloop.index }},
            "selected_value": {{ option.selected_value | json }},
            "values": [
              {% for v in option.values %}
                {{ v | json }}
                {% unless forloop.last %},{% endunless %}
              {% endfor %}
            ]
          }
          {% unless forloop.last %},{% endunless %}
        {% endfor %}
      ]
    }
  {% endcapture %}

  {{ product_json | strip_newlines | strip }}

{% endif %}
