# Implementation Plans

Generated by the improve skill on 2026-07-29. Execute the plans in the order
below unless dependencies say otherwise. Each executor should read the plan
fully, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make product variant updates type-safe for incomplete selections | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- Plan 001 has no dependencies. It is intentionally limited to the optional
  variant contract used by the product form and its two direct UI consumers.

## Findings considered and rejected

- Enabling `strict` in `tsconfig.json`: rejected for this focused plan because
  the current repo-wide strict check reports many unrelated errors; making that
  migration safe requires a separate inventory and staged plan.
- Adding a runtime schema validator for the `JSON.parse` product payload:
  deferred because it is a worthwhile type-safety improvement but is not needed
  to resolve the three strict diagnostics in the requested form flow.
- Adding a test runner: rejected for this plan because `package.json` has no
  configured test framework and the requested change is a compile-time contract
  alignment with unchanged runtime branches.
